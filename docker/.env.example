# Required for Docker build (repo cloning + ChunkHound indexing)
GITHUB_PAT=ghp_your_github_personal_access_token
OPENAI_API_KEY=sk-your_openai_api_key_for_embeddings

# Required at runtime (LLM provider)
ANTHROPIC_API_KEY=sk-ant-your_anthropic_api_key

# Optional: Override default gateway token (default: swanson-dev-token)
OPENCLAW_GATEWAY_TOKEN=swanson-dev-token
